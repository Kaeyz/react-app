// modules
import React from 'react';
import styled from 'styled-components';


const Wrapper = styled.div`

	/* Officer message styling goes here */

`;

export default function OfficerMessage() {
	return (
		<Wrapper>
			{/* Officer message component goes here */}
			OfficerMessage
		</Wrapper>
	);
}

OfficerMessage.propTypes = {
	//TODO: Officer message prop types goes here
};